# Lazy-evaluation audit allowlist.
#
# Each entry documents an exception to Archetype's lazy execution
# contract. Execution-capable terminals run a Daft DataFrame plan;
# conversion terminals may also force it through Python memory and
# defeat query planning. Adding an entry here is auditable: PR reviewers
# will challenge any new entry whose reason does not name a specific
# technical boundary (storage write, transport conversion, single-row
# migration extract).
#
# Generic reasons ("needed", "for the test", "convenience") are
# rejected automatically by scripts/check_lazy_audit.py.
#
# Entries are keyed by (path, symbol, expr, method) — never by line
# number. `symbol` is the dotted path of the enclosing def/class (or
# "<module>"); `expr` is the normalized source of the audited attribute
# access. An edit elsewhere in the file cannot invalidate an entry, and
# reformatting the call cannot either, because `expr` comes from
# ast.unparse. Editing the audited expression itself does re-open review,
# which is the intent. Run `python scripts/check_lazy_audit.py --list` to
# see the current key for every site.
#
# Scope: src/ only. Tests are exempt — terminal materialization at the
# assertion boundary is expected and not part of the audited contract.
#
# **Sanctioned exemption — batch-UDF Series access**
#
# Series.to_pylist() called on a *parameter* of a function decorated
# with @daft.method.batch or @daft.func.batch does NOT need an entry
# here. When Daft invokes such a function the batch is already
# materialised by the executor — converting parameters to Python lists
# is the expected interface at that boundary. The checker detects this
# pattern via AST analysis and reports these sites separately as
# "udf-boundary (sanctioned)". See scripts/check_lazy_audit.py for
# the full detection logic.

[[entries]]
path = "src/archetype/api/models.py"
symbol = "dataframe_to_rows"
expr = "df.collect"
method = "collect"
reason = "HTTP JSON response boundary: FastAPI must receive concrete row dictionaries for response encoding; a Daft lazy DataFrame cannot be represented as JSON without executing the query."

[[entries]]
path = "src/archetype/api/models.py"
symbol = "dataframe_to_rows"
expr = "df.collect().to_pylist"
method = "to_pylist"
reason = "HTTP JSON response boundary: convert the already-collected Daft result into row dictionaries for FastAPI's JSON encoder at the terminal REST boundary."

[[entries]]
path = "src/archetype/physical_ai/views.py"
symbol = "latest_rows"
expr = "materialized.to_pylist"
method = "to_pylist"
reason = "Physical-evaluation grading boundary: StorageService admits and materializes latest-tick ledger rows before bounded success and episode-length values become typed task or sweep reports."

[[entries]]
path = "src/archetype/research/views.py"
symbol = "next_iteration"
expr = "materialized.to_pylist"
method = "to_pylist"
reason = "Bounded control-plane history extract: StorageService has already admitted and materialized the query; deterministic Run ids and statuses then enter Python control flow to reject ambiguous resume state."

[[entries]]
path = "src/archetype/research/views.py"
symbol = "read_experiment"
expr = "materialized.to_pylist"
method = "to_pylist"
reason = "Single-row experiment-contract extract: StorageService has already admitted and materialized the query; the persisted identity and JSON configuration digest then enter Python control flow for exact collision rejection before resume."

[[entries]]
path = "src/archetype/research/views.py"
symbol = "read_head"
expr = "materialized.to_pylist"
method = "to_pylist"
reason = "Single-row loop-state extract: StorageService has already admitted and materialized the query; the latest BranchHead then enters Python control flow for incumbent comparison and entity-id routing."

[[entries]]
path = "src/archetype/core/aio/async_store.py"
symbol = "AsyncStore.append"
expr = "df.collect"
method = "collect"
reason = "Storage write boundary: materialize the incoming frame once for the empty-row guard and reuse that same materialized frame for the backing-table append."

[[entries]]
path = "src/archetype/core/aio/async_world.py"
symbol = "AsyncWorld._move_entity"
expr = "df.to_pylist"
method = "to_pylist"
reason = "Cross-archetype entity migration: extract the latest tick row as a Python dict for component-overlay, which mutates field-by-field before re-insertion into the new archetype."

[[entries]]
path = "src/archetype/core/lineage.py"
symbol = "load_lineage"
expr = "df.to_pylist"
method = "to_pylist"
reason = "Lineage metadata extract: a fork's ancestor segments (bounded by fork depth, typically 1-3 rows) must become Python (world_id, run_id, up_to_tick) tuples because they route subsequent per-tick store queries; the routing decision cannot be expressed inside a single Daft plan."

[[entries]]
path = "src/archetype/core/sync/world.py"
symbol = "SyncWorld._move_entity"
expr = "df.to_pylist"
method = "to_pylist"
reason = "Cross-archetype entity migration (sync mirror of async_world): extract the latest tick row for component overlay before re-insertion."

[[entries]]
path = "src/archetype/graph/cascade.py"
symbol = "_plan"
expr = "dangling_edges(edges, rel, population).select(col('entity_id'), col(f'{prefix}source').alias('source')).to_pylist"
method = "to_pylist"
reason = "Mutation-planning boundary for cascade: despawn takes concrete ids, so _plan materializes the (edge id, source) pairs of the dangling edges; the liveness union, anti-join, and filters all run in the lazy plan and only ids cross into Python."

[[entries]]
path = "src/archetype/graph/frames.py"
symbol = "_ids_at"
expr = "edges.where(at_latest).select('entity_id').to_pylist"
method = "to_pylist"
reason = "Mutation-planning boundary shared by async and sync unlink: despawn takes concrete entity ids, not a frame, so _ids_at (shared by unlink and exclusive link replacement) materializes the matching ids at the latest persisted tick; all filters run in the lazy plan and only ids cross into Python."

[[entries]]
path = "src/archetype/graph/prefab.py"
symbol = "_rows"
expr = "frame.to_pylist"
method = "to_pylist"
reason = "Mutation-planning boundary for instantiation: spawning copies requires the template's concrete component values and child prefab ids; _rows is the single funnel and every entity filter runs in the lazy plan before it."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_critic_activity_intents"
expr = "author_executions.to_pylist"
method = "to_pylist"
reason = "Durable critic-Activity admission boundary: persisted author execution identities and sandbox ids become bounded scalar fields in the exact-candidate request admitted only after the source tick commits."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "_critic_subject_bounds"
expr = "frame.to_pylist"
method = "to_pylist"
reason = "Durable critic-Activity admission boundary: the task-scoped companion policy is already component-filtered and its one bounded integer becomes part of the immutable exact-subject request rather than provider process state."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_complete_author_activity_observations"
expr = "executions.to_pylist"
method = "to_pylist"
reason = "Exact post-commit activity-settlement boundary: finite committed AgentExecution facts become scalar identities used to verify that an author result bundle is complete before acknowledging its bounded durable result."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_complete_author_activity_observations"
expr = "validations.to_pylist"
method = "to_pylist"
reason = "Exact post-commit activity-settlement boundary: finite committed ValidationResult facts are counted against the result-bound completeness marker before the activity catalog can acknowledge observation."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_complete_author_activity_observations"
expr = "commits.to_pylist"
method = "to_pylist"
reason = "Exact post-commit activity-settlement boundary: finite committed Commit facts are counted against the result-bound completeness marker before the activity catalog can acknowledge observation."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_complete_author_activity_observations"
expr = "friction.to_pylist"
method = "to_pylist"
reason = "Exact post-commit activity-settlement boundary: finite committed FrictionLog facts are counted against the result-bound completeness marker before the activity catalog can acknowledge observation."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_complete_author_activity_observations"
expr = "markers.to_pylist"
method = "to_pylist"
reason = "Exact post-commit activity-settlement boundary: bounded AuthorActivityObservation markers become scalar result references, digests, identities, and expected fact counts for catalog settlement."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "_component_facts"
expr = "frame.to_pylist"
method = "to_pylist"
reason = "Exact post-commit Activity fact boundary: each already component-scoped committed frame becomes a bounded set of typed Mission facts so entity identities, provenance edges, and the v2 completion digest can be verified before settlement."

[[entries]]
path = "src/archetype/app/missions/activity_world.py"
symbol = "WorldMissionAuthorObservationStager._committed_bundle"
expr = "materialized_markers.to_pylist"
method = "to_pylist"
reason = "Idempotent Activity-delivery boundary: the exact manifest-pinned AuthorActivityObservation frame becomes bounded marker identities so reconstruction can reject conflicting results or avoid duplicate world mutations."

[[entries]]
path = "src/archetype/app/missions/critic_activity_world.py"
symbol = "WorldMissionCriticObservationStager._committed_bundle"
expr = "materialized_markers.to_pylist"
method = "to_pylist"
reason = "Idempotent critic-Activity delivery boundary: the exact manifest-pinned completion-marker frame becomes bounded activity identities so durable-result reconstruction can reject conflicts or avoid duplicate world mutations after restart."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "_critic_component_facts"
expr = "frame.to_pylist"
method = "to_pylist"
reason = "Exact post-commit critic-Activity fact boundary: each already component-scoped committed frame becomes a bounded set of typed facts so entity identities, provenance edges, and the result-bound completion digest can be verified before settlement."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_critic_activity_intents"
expr = "candidate_tasks.to_pylist"
method = "to_pylist"
reason = "Durable critic-Activity admission boundary: the frame is already restricted to committed CANDIDATE task rows, whose bounded histories must be reduced independently before matching immutable candidates; a Daft join of the two snapshot histories crashes the native runtime during the real closed-loop mission."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_critic_activity_intents"
expr = "candidates.to_pylist"
method = "to_pylist"
reason = "Durable critic-Activity admission boundary: bounded committed candidate histories must retain their entity identities while they are reduced and matched to current task rows; provider execution occurs only after the resulting exact request is admitted."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_critic_activity_intents"
expr = "critic_executions.to_pylist"
method = "to_pylist"
reason = "Committed domain-budget boundary: finite CriticExecution facts are counted to assign the next semantic review attempt; generic Activity lease attempts are intentionally absent and cannot consume this budget."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_critic_activity_intents"
expr = "critic_receipts.to_pylist"
method = "to_pylist"
reason = "Durable review-admission boundary: bounded committed CriticReceipt identities are compared with the exact candidate before admitting more consequential work, preventing a completed review from being invoked twice."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_critic_activity_intents"
expr = "validation_results.to_pylist"
method = "to_pylist"
reason = "Durable critic-Activity admission boundary: exact-revision ValidationResult observations become bounded immutable evidence in the request value after candidate state has committed."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_critic_activity_intents"
expr = "validators.to_pylist"
method = "to_pylist"
reason = "Durable critic-Activity admission boundary: finite TaskValidator definitions become concrete command tuples in the immutable request; the generic catalog and external provider cannot consume a lazy DataFrame."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "TaskDispatchOutbox.on_post_tick"
expr = "candidate_history.to_pylist"
method = "to_pylist"
reason = "Repair-dispatch external-I/O boundary: persisted candidate identities are materialized after the committed DISPATCHED-task projection so the application can select the immediately superseded candidate whose durable critic findings belong in the next coding-agent request."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "TaskDispatchOutbox.on_post_tick"
expr = "dispatched.to_pylist"
method = "to_pylist"
reason = "Post-commit external-I/O boundary: the lazy frame is restricted to committed DISPATCHED tasks and joined to mission identity before those bounded intents become Python requests for sandbox processes; an OS process invocation cannot remain a Daft expression."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "TaskDispatchOutbox.on_post_tick"
expr = "finding_history.to_pylist"
method = "to_pylist"
reason = "Repair-dispatch external-I/O boundary: bounded redacted CriticFinding values must become concrete typed prompt input for the next author process; an external coding-agent request cannot consume a lazy Daft frame."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "TaskDispatchOutbox.on_post_tick"
expr = "guarded_validators.to_pylist"
method = "to_pylist"
reason = "Post-commit validator-command boundary: Guards and TaskValidator entities are joined lazily first, then the finite validators attached to dispatchable tasks become argv tuples consumed by the repository harness; process argv cannot remain a DataFrame value."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "TaskDispatchOutbox.on_post_tick"
expr = "observations.to_pylist"
method = "to_pylist"
reason = "Repair-turn boundary: prior AgentExecution and ValidationResult entities are joined lazily by execution before the finite previous-dispatch observations become a coding-agent prompt and session-resume request; the external driver requires concrete typed observations."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "current_mission_status"
expr = "frame.where(cast(Expression, col('entity_id') == mission_id)).select(f'{state}status').to_pylist"
method = "to_pylist"
reason = "Single-mission control-loop boundary: the frame is filtered to one submitted mission and only its persisted status enters Python to decide whether the application service should tick again or return; no mission payload rows are materialized."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_mission_result"
expr = "mission_frame.where(cast(Expression, col('entity_id') == submitted.mission_id)).to_pylist"
method = "to_pylist"
reason = "Terminal mission result boundary: the frame is filtered to one terminal mission whose scalar repository, branch, status, and reason must become the immutable MissionResult returned by the public runtime API."

[[entries]]
path = "src/archetype/missions/projections.py"
symbol = "project_mission_result"
expr = "task_projection.to_pylist"
method = "to_pylist"
reason = "Terminal task result boundary: task state is filtered to the submitted ids and left-joined lazily to first-class commit entities before the finite scalar rows become immutable TaskResult values returned by the public runtime API."

[[entries]]
path = "src/archetype/physical_ai/boundary.py"
symbol = "series_to_rows"
expr = "s.to_pylist"
method = "to_pylist"
reason = "series_to_rows helper: converts each Series parameter to a Python list inside a list comprehension. This function is called exclusively from @daft.method.batch UDF bodies (MuJoCo C-library boundary, env RPC boundary, policy inference boundary) where the Daft executor has already materialised the batch. The AST checker auto-exempts to_pylist calls on named parameters of @daft.method.batch functions but cannot trace through this plain helper indirection; the call is semantically equivalent to an auto-sanctioned UDF-boundary access."

[[entries]]
path = "src/archetype/physical_ai/hosted_episode.py"
symbol = "_decode_rows"
expr = "batches[0].to_pylist"
method = "to_pylist"
reason = "Canonical Arrow IPC codec boundary: the input is already one validated in-memory PyArrow RecordBatch, not a lazy Daft DataFrame, and exact Python values are required to validate cross-payload identities, terminal completeness, and canonical byte re-encoding before any Activity result reference can be recorded."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_missing"
expr = "aligned.collect"
method = "collect"
reason = "Conditional producer boundary: StorageService freezes the aligned candidate plan once so duplicate-key validation and optimistic Iceberg retries cannot re-run external or nondeterministic expressions."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_missing"
expr = "pending.collect"
method = "collect"
reason = "Conditional append boundary: StorageService materializes the anti-join after each refreshed Iceberg snapshot so a conflicting writer cannot cause a stale retry to duplicate an existing logical key."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_table"
expr = "aligned.collect"
method = "collect"
reason = "Conflict-safe append boundary: StorageService materializes producer rows exactly once so optimistic Iceberg retries can reuse the same payload instead of re-running external or nondeterministic expressions."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.materialize"
expr = "frame.collect"
method = "collect"
reason = "Central application execution boundary: StorageService admits the caller's lazy Daft plan through its shared execution gate, materializes it off the event loop, and returns the completed frame."

[[entries]]
path = "src/archetype/world/resume.py"
symbol = "_merge_visible_segment"
expr = "materialized.to_pylist"
method = "to_pylist"
reason = "Resume-time entity-directory extract (issue #273 A1-resume): StorageService admits and materializes the groupby-max result before bounded live-entity rows become the imperative entity2sig registry and next_entity_id counter."

[[entries]]
path = "src/archetype/world/simulation.py"
symbol = "_entities_terminal"
expr = "materialized.to_pylist"
method = "to_pylist"
reason = "Episode-termination boundary: StorageService admits and materializes the one-row (n_done, n_total) reduction before its all/any values enter Python control flow to stop the episode loop."

[[entries]]
path = "src/archetype/api/routes/query.py"
symbol = "get_components"
expr = "frame.count_rows"
method = "count_rows"
reason = "Explicit HTTP count operation: the caller requested a scalar count instead of row output, so the API route owns this terminal query boundary."

[[entries]]
path = "src/archetype/artifacts/handlers.py"
symbol = "ingest_artifacts"
expr = "discovered.to_pydict"
method = "to_pydict"
reason = "Artifact discovery validation boundary: StorageService has already materialized the occurrence frame once; bounded source identities enter Python validation before any durable publication."

[[entries]]
path = "src/archetype/artifacts/handlers.py"
symbol = "ingest_artifacts"
expr = "stored.to_pydict"
method = "to_pydict"
reason = "Artifact index-routing boundary: StorageService has already materialized the persisted occurrence frame once; bounded media families and logical paths select the required typed indexes."

[[entries]]
path = "src/archetype/core/aio/async_cached_store.py"
symbol = "AsyncCachedStore.append"
expr = "df.to_arrow_iter"
method = "to_arrow_iter"
reason = "Cached-store append boundary: stream the incoming Daft plan once into bounded Arrow record batches retained until the owning durable flush."

[[entries]]
path = "src/archetype/core/aio/async_lancedb_store.py"
symbol = "AsyncLancedbStore.get_existing_table_df"
expr = "table.query().where(where_str).to_arrow"
method = "to_arrow"
reason = "LanceDB SDK query boundary: the async SDK returns an Arrow table for the exact durable table identity; the receiver is not a lazy Daft DataFrame."

[[entries]]
path = "src/archetype/core/aio/async_lancedb_store.py"
symbol = "AsyncLancedbStore.get_archetype_df"
expr = "async_table.query().where(where_str).to_arrow"
method = "to_arrow"
reason = "LanceDB SDK query boundary: execute the backend-filtered async query once and convert its Arrow result into the lazy Daft frame returned to core."

[[entries]]
path = "src/archetype/core/aio/async_lancedb_store.py"
symbol = "AsyncLancedbStore._legacy_df"
expr = "table.query().where(where_str).to_arrow"
method = "to_arrow"
reason = "LanceDB SDK legacy-read boundary: execute the backend-filtered frozen-history query once before aligning its Arrow result to the current lazy Daft schema."

[[entries]]
path = "src/archetype/core/aio/async_lancedb_store.py"
symbol = "AsyncLancedbStore.append"
expr = "df.to_arrow"
method = "to_arrow"
reason = "LanceDB append boundary: materialize the incoming Daft plan exactly once as the Arrow table required by the backend add operation and its returned row metadata."

[[entries]]
path = "src/archetype/core/aio/async_store.py"
symbol = "AsyncStore._append_table"
expr = "df.write_iceberg"
method = "write_iceberg"
reason = "Iceberg storage sink: explicit IO configuration requires the native Daft write at the store-owned append boundary."

[[entries]]
path = "src/archetype/core/aio/async_store.py"
symbol = "AsyncStore.append"
expr = "materialized.count_rows"
method = "count_rows"
reason = "Owned store boundary: the input is already collected once; this count reads the concrete result to preserve empty-append and committed-signature behavior without replaying the upstream plan."

[[entries]]
path = "src/archetype/core/sync/store.py"
symbol = "SyncStore._append_table"
expr = "df.write_iceberg"
method = "write_iceberg"
reason = "Synchronous Iceberg storage sink: explicit IO configuration requires the native Daft write at the store-owned append boundary."

[[entries]]
path = "src/archetype/evaluation/views.py"
symbol = "read_result"
expr = "(await storage.materialize(matched)).to_pydict"
method = "to_pydict"
reason = "Single-row evaluation receipt boundary: StorageService admits and materializes the exact identity match before its bounded scalar values become the typed return value."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.scan_visible_world_rows"
expr = "tick_frame.to_pydict"
method = "to_pydict"
reason = "Single-row physical tick reduction: StorageService has already materialized the aggregate, whose bounded maximum tick routes visibility metadata for the returned table frame."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_table"
expr = "frozen.count_rows"
method = "count_rows"
reason = "Owned append receipt boundary: the producer plan is already collected exactly once, and the concrete row count controls the write no-op and returned scalar without replay."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_table"
expr = "frozen.write_iceberg"
method = "write_iceberg"
reason = "StorageService-owned Iceberg sink: write the single frozen producer result while retaining it across bounded optimistic commit retries."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_missing"
expr = "candidates.count_rows"
method = "count_rows"
reason = "Conditional-write validation boundary: the candidate plan is already collected once, and its concrete row count is compared with the key projection before persistence."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_missing"
expr = "candidates.where(null_key_filter).limit(1).count_rows"
method = "count_rows"
reason = "Conditional-write validation boundary: execute a bounded one-row null-key probe over frozen candidates before admitting them to the Iceberg sink."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_missing"
expr = "candidates.distinct(*key_columns).count_rows"
method = "count_rows"
reason = "Conditional-write validation boundary: count distinct keys over frozen candidates to reject ambiguous same-batch identities before persistence."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_missing"
expr = "pending.count_rows"
method = "count_rows"
reason = "Owned append receipt boundary: the anti-joined pending frame is already collected once, and its concrete row count controls the write no-op and returned scalar."

[[entries]]
path = "src/archetype/storage/service.py"
symbol = "StorageService.append_missing"
expr = "pending.write_iceberg"
method = "write_iceberg"
reason = "StorageService-owned conditional Iceberg sink: write the frozen anti-joined rows under the execution gate and bounded optimistic commit retry."
