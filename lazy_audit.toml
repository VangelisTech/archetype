# Daft lazy-execution terminal inventory.
#
# Every checked-in Daft execution, materialization, streaming, export, display,
# or commit boundary discovered from the version-pinned registry has one exact
# path/method/boundary_kind/owner/rationale disposition below. Entries are not
# permission to add execution: keep plans lazy until an already-owned boundary.
# Legacy entries must name the issue that removes them. Tests, benchmarks, evals,
# examples, experiments, scripts, and quality tooling are audited with product code.
#
# The sole sanctioned non-entry is Series.to_pylist() on a parameter of a
# @daft.method.batch or @daft.func.batch function: Daft already owns execution.

[[entries]]
path = "src/archetype/api/models.py"
line = 33
method = "collect"
boundary_kind = "transport-serialization"
owner = "src/archetype/api"
rationale = "HTTP JSON response boundary: FastAPI must receive concrete row dictionaries for response encoding; a Daft lazy DataFrame cannot be represented as JSON without executing the query."

[[entries]]
path = "src/archetype/api/models.py"
line = 33
method = "to_pylist"
boundary_kind = "transport-serialization"
owner = "src/archetype/api"
rationale = "HTTP JSON response boundary: convert the already-collected Daft result into row dictionaries for FastAPI's JSON encoder at the terminal REST boundary."

[[entries]]
path = "src/archetype/api/routes/query.py"
line = 252
method = "count_rows"
boundary_kind = "transport-serialization"
owner = "src/archetype/api/routes"
rationale = "Owned transport serialization in src/archetype/api/routes/query.py: the Daft count_rows terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/app/artifacts/service.py"
line = 103
method = "to_pydict"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/artifacts"
rationale = "Owned control flow extract in src/archetype/app/artifacts/service.py: the Daft to_pydict terminal produces the bounded concrete value required by this application boundary."

[[entries]]
path = "src/archetype/app/artifacts/service.py"
line = 119
method = "to_pydict"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/artifacts"
rationale = "Owned control flow extract in src/archetype/app/artifacts/service.py: the Daft to_pydict terminal produces the bounded concrete value required by this application boundary."

[[entries]]
path = "src/archetype/app/evaluation/service.py"
line = 404
method = "to_pydict"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/evaluation"
rationale = "Owned control flow extract in src/archetype/app/evaluation/service.py: the Daft to_pydict terminal produces the bounded concrete value required by this application boundary."

[[entries]]
path = "src/archetype/app/physical_ai/service.py"
line = 108
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/physical_ai"
rationale = "Physical-evaluation grading boundary: StorageService admits and materializes latest-tick ledger rows before bounded success and episode-length values become typed task or sweep reports."

[[entries]]
path = "src/archetype/app/query/service.py"
line = 409
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/query"
rationale = "Bounded compatibility boundary: StorageService admits and materializes already filtered, limited audit rows before conversion into the legacy list[Command] return contract."

[[entries]]
path = "src/archetype/app/research/service.py"
line = 465
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/research"
rationale = "Single-row experiment-contract extract: StorageService has already admitted and materialized the query; the persisted identity and JSON configuration digest then enter Python control flow for exact collision rejection before resume."

[[entries]]
path = "src/archetype/app/research/service.py"
line = 477
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/research"
rationale = "Single-row loop-state extract: StorageService has already admitted and materialized the query; the latest BranchHead then enters Python control flow for incumbent comparison and entity-id routing."

[[entries]]
path = "src/archetype/app/research/service.py"
line = 494
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/research"
rationale = "Bounded control-plane history extract: StorageService has already admitted and materialized the query; deterministic Run ids and statuses then enter Python control flow to reject ambiguous resume state."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 429
method = "collect"
boundary_kind = "storage-execution"
owner = "src/archetype/app/storage"
rationale = "Central application execution boundary: StorageService admits the caller's lazy Daft plan through its shared execution gate, materializes it off the event loop, and returns the completed frame."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 465
method = "collect"
boundary_kind = "storage-execution"
owner = "src/archetype/app/storage"
rationale = "Conflict-safe append boundary: StorageService materializes producer rows exactly once so optimistic Iceberg retries can reuse the same payload instead of re-running external or nondeterministic expressions."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 468
method = "count_rows"
boundary_kind = "execution-boundary"
owner = "src/archetype/app/storage"
rationale = "Owned execution boundary in src/archetype/app/storage/service.py: the Daft count_rows terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 471
method = "write_iceberg"
boundary_kind = "storage-commit"
owner = "src/archetype/app/storage"
rationale = "Owned storage commit in src/archetype/app/storage/service.py: the Daft write_iceberg terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 517
method = "collect"
boundary_kind = "storage-execution"
owner = "src/archetype/app/storage"
rationale = "Conditional producer boundary: StorageService freezes the aligned candidate plan once so duplicate-key validation and optimistic Iceberg retries cannot re-run external or nondeterministic expressions."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 520
method = "count_rows"
boundary_kind = "execution-boundary"
owner = "src/archetype/app/storage"
rationale = "Owned execution boundary in src/archetype/app/storage/service.py: the Daft count_rows terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 525
method = "count_rows"
boundary_kind = "execution-boundary"
owner = "src/archetype/app/storage"
rationale = "Owned execution boundary in src/archetype/app/storage/service.py: the Daft count_rows terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 534
method = "count_rows"
boundary_kind = "execution-boundary"
owner = "src/archetype/app/storage"
rationale = "Owned execution boundary in src/archetype/app/storage/service.py: the Daft count_rows terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 544
method = "collect"
boundary_kind = "storage-execution"
owner = "src/archetype/app/storage"
rationale = "Conditional append boundary: StorageService materializes the anti-join after each refreshed Iceberg snapshot so a conflicting writer cannot cause a stale retry to duplicate an existing logical key."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 547
method = "count_rows"
boundary_kind = "execution-boundary"
owner = "src/archetype/app/storage"
rationale = "Owned execution boundary in src/archetype/app/storage/service.py: the Daft count_rows terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/app/storage/service.py"
line = 556
method = "write_iceberg"
boundary_kind = "storage-commit"
owner = "src/archetype/app/storage"
rationale = "Owned storage commit in src/archetype/app/storage/service.py: the Daft write_iceberg terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/app/world/service.py"
line = 742
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/world"
rationale = "Resume-time entity-directory extract (issue #273 A1-resume): StorageService admits and materializes the groupby-max result before bounded live-entity rows become the imperative entity2sig registry and next_entity_id counter."

[[entries]]
path = "src/archetype/app/world/simulation.py"
line = 228
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/app/world"
rationale = "Episode-termination boundary: StorageService admits and materializes the one-row (n_done, n_total) reduction before its all/any values enter Python control flow to stop the episode loop."

[[entries]]
path = "src/archetype/core/aio/async_cached_store.py"
line = 276
method = "to_arrow_iter"
boundary_kind = "storage-execution"
owner = "src/archetype/core/aio"
rationale = "Owned storage execution in src/archetype/core/aio/async_cached_store.py: the Daft to_arrow_iter terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/aio/async_lancedb_store.py"
line = 201
method = "to_arrow"
boundary_kind = "execution-boundary"
owner = "src/archetype/core/aio"
rationale = "Owned execution boundary in src/archetype/core/aio/async_lancedb_store.py: the Daft to_arrow terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/aio/async_lancedb_store.py"
line = 288
method = "to_arrow"
boundary_kind = "execution-boundary"
owner = "src/archetype/core/aio"
rationale = "Owned execution boundary in src/archetype/core/aio/async_lancedb_store.py: the Daft to_arrow terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/aio/async_lancedb_store.py"
line = 333
method = "to_arrow"
boundary_kind = "execution-boundary"
owner = "src/archetype/core/aio"
rationale = "Owned execution boundary in src/archetype/core/aio/async_lancedb_store.py: the Daft to_arrow terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/aio/async_lancedb_store.py"
line = 359
method = "to_arrow"
boundary_kind = "storage-execution"
owner = "src/archetype/core/aio"
rationale = "Owned storage execution in src/archetype/core/aio/async_lancedb_store.py: the Daft to_arrow terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/aio/async_store.py"
line = 95
method = "append"
boundary_kind = "storage-commit"
owner = "src/archetype/core/aio"
rationale = "Owned storage commit in src/archetype/core/aio/async_store.py: the Daft append terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/aio/async_store.py"
line = 101
method = "append"
boundary_kind = "storage-commit"
owner = "src/archetype/core/aio"
rationale = "Owned storage commit in src/archetype/core/aio/async_store.py: the Daft append terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/aio/async_store.py"
line = 104
method = "write_iceberg"
boundary_kind = "storage-commit"
owner = "src/archetype/core/aio"
rationale = "Owned storage commit in src/archetype/core/aio/async_store.py: the Daft write_iceberg terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/aio/async_store.py"
line = 273
method = "collect"
boundary_kind = "storage-execution"
owner = "src/archetype/core/aio"
rationale = "Storage write boundary: materialize the incoming frame once for the empty-row guard and reuse that same materialized frame for the backing-table append."

[[entries]]
path = "src/archetype/core/aio/async_store.py"
line = 274
method = "count_rows"
boundary_kind = "legacy"
owner = "src/archetype/core/aio"
rationale = "Temporary core row-count execution in src/archetype/core/aio/async_store.py; issue #538 owns removal because count_rows can execute and replay the lazy plan even when used as a preflight or diagnostic."
removal_issue = "#538"

[[entries]]
path = "src/archetype/core/aio/async_system.py"
line = 106
method = "count_rows"
boundary_kind = "legacy"
owner = "src/archetype/core/aio"
rationale = "Temporary core row-count execution in src/archetype/core/aio/async_system.py; issue #538 owns removal because count_rows can execute and replay the lazy plan even when used as a preflight or diagnostic."
removal_issue = "#538"

[[entries]]
path = "src/archetype/core/aio/async_world.py"
line = 422
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/core/aio"
rationale = "Cross-archetype entity migration: extract the latest tick row as a Python dict for component-overlay, which mutates field-by-field before re-insertion into the new archetype."

[[entries]]
path = "src/archetype/core/aio/async_world.py"
line = 994
method = "count_rows"
boundary_kind = "legacy"
owner = "src/archetype/core/aio"
rationale = "Temporary core row-count execution in src/archetype/core/aio/async_world.py; issue #538 owns removal because count_rows can execute and replay the lazy plan even when used as a preflight or diagnostic."
removal_issue = "#538"

[[entries]]
path = "src/archetype/core/lineage.py"
line = 105
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/core"
rationale = "Lineage metadata extract: a fork's ancestor segments (bounded by fork depth, typically 1-3 rows) must become Python (world_id, run_id, up_to_tick) tuples because they route subsequent per-tick store queries; the routing decision cannot be expressed inside a single Daft plan."

[[entries]]
path = "src/archetype/core/sync/querier.py"
line = 79
method = "count_rows"
boundary_kind = "legacy"
owner = "src/archetype/core/sync"
rationale = "Temporary core row-count execution in src/archetype/core/sync/querier.py; issue #538 owns removal because count_rows can execute and replay the lazy plan even when used as a preflight or diagnostic."
removal_issue = "#538"

[[entries]]
path = "src/archetype/core/sync/querier.py"
line = 81
method = "show"
boundary_kind = "legacy"
owner = "src/archetype/core/sync"
rationale = "Temporary core diagnostic execution in src/archetype/core/sync/querier.py; issue #530 owns removal because diagnostics must not introduce a separate Daft terminal."
removal_issue = "#530"

[[entries]]
path = "src/archetype/core/sync/store.py"
line = 95
method = "append"
boundary_kind = "storage-commit"
owner = "src/archetype/core/sync"
rationale = "Owned storage commit in src/archetype/core/sync/store.py: the Daft append terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/sync/store.py"
line = 101
method = "append"
boundary_kind = "storage-commit"
owner = "src/archetype/core/sync"
rationale = "Owned storage commit in src/archetype/core/sync/store.py: the Daft append terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/sync/store.py"
line = 104
method = "write_iceberg"
boundary_kind = "storage-commit"
owner = "src/archetype/core/sync"
rationale = "Owned storage commit in src/archetype/core/sync/store.py: the Daft write_iceberg terminal produces the concrete value or durable effect required at this named boundary."

[[entries]]
path = "src/archetype/core/sync/store.py"
line = 116
method = "show"
boundary_kind = "legacy"
owner = "src/archetype/core/sync"
rationale = "Temporary core diagnostic execution in src/archetype/core/sync/store.py; issue #530 owns removal because diagnostics must not introduce a separate Daft terminal."
removal_issue = "#530"

[[entries]]
path = "src/archetype/core/sync/store.py"
line = 132
method = "collect"
boundary_kind = "legacy"
owner = "src/archetype/core/sync"
rationale = "Temporary debug-only materialization before diagnostic logging; issue #530 owns removal because diagnostics must remain a semantic no-op and cannot execute a separate plan."
removal_issue = "#530"

[[entries]]
path = "src/archetype/core/sync/store.py"
line = 133
method = "count_rows"
boundary_kind = "legacy"
owner = "src/archetype/core/sync"
rationale = "Temporary core row-count execution in src/archetype/core/sync/store.py; issue #538 owns removal because count_rows can execute and replay the lazy plan even when used as a preflight or diagnostic."
removal_issue = "#538"

[[entries]]
path = "src/archetype/core/sync/store.py"
line = 134
method = "show"
boundary_kind = "legacy"
owner = "src/archetype/core/sync"
rationale = "Temporary core diagnostic execution in src/archetype/core/sync/store.py; issue #530 owns removal because diagnostics must not introduce a separate Daft terminal."
removal_issue = "#530"

[[entries]]
path = "src/archetype/core/sync/world.py"
line = 305
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/core/sync"
rationale = "Cross-archetype entity migration (sync mirror of async_world): extract the latest tick row for component overlay before re-insertion."

[[entries]]
path = "src/archetype/graph/cascade.py"
line = 94
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/graph"
rationale = "Mutation-planning boundary for cascade: despawn takes concrete ids, so _plan materializes the (edge id, source) pairs of the dangling edges; the liveness union, anti-join, and filters all run in the lazy plan and only ids cross into Python."

[[entries]]
path = "src/archetype/graph/frames.py"
line = 65
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/graph"
rationale = "Mutation-planning boundary shared by async and sync unlink: despawn takes concrete entity ids, not a frame, so _ids_at (shared by unlink and exclusive link replacement) materializes the matching ids at the latest persisted tick; all filters run in the lazy plan and only ids cross into Python."

[[entries]]
path = "src/archetype/graph/prefab.py"
line = 68
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/graph"
rationale = "Mutation-planning boundary for instantiation: spawning copies requires the template's concrete component values and child prefab ids; _rows is the single funnel and every entity filter runs in the lazy plan before it."

[[entries]]
path = "src/archetype/missions/projections.py"
line = 109
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/missions"
rationale = "Post-commit external-I/O boundary: the lazy frame is restricted to committed DISPATCHED tasks and joined to mission identity before those bounded intents become Python requests for sandbox processes; an OS process invocation cannot remain a Daft expression."

[[entries]]
path = "src/archetype/missions/projections.py"
line = 135
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/missions"
rationale = "Post-commit validator-command boundary: Guards and TaskValidator entities are joined lazily first, then the finite validators attached to dispatchable tasks become argv tuples consumed by the repository harness; process argv cannot remain a DataFrame value."

[[entries]]
path = "src/archetype/missions/projections.py"
line = 176
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/missions"
rationale = "Repair-turn boundary: prior AgentExecution and ValidationResult entities are joined lazily by execution before the finite previous-dispatch observations become a coding-agent prompt and session-resume request; the external driver requires concrete typed observations."

[[entries]]
path = "src/archetype/missions/projections.py"
line = 259
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/missions"
rationale = "Single-mission control-loop boundary: the frame is filtered to one submitted mission and only its persisted status enters Python to decide whether the application service should tick again or return; no mission payload rows are materialized."

[[entries]]
path = "src/archetype/missions/projections.py"
line = 281
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/missions"
rationale = "Terminal mission result boundary: the frame is filtered to one terminal mission whose scalar repository, branch, status, and reason must become the immutable MissionResult returned by the public runtime API."

[[entries]]
path = "src/archetype/missions/projections.py"
line = 300
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/missions"
rationale = "Terminal task result boundary: task state is filtered to the submitted ids and left-joined lazily to first-class commit entities before the finite scalar rows become immutable TaskResult values returned by the public runtime API."

[[entries]]
path = "src/archetype/physical_ai/boundary.py"
line = 122
method = "to_pylist"
boundary_kind = "control-flow-extract"
owner = "src/archetype/physical_ai"
rationale = "series_to_rows helper: converts each Series parameter to a Python list inside a list comprehension. This function is called exclusively from @daft.method.batch UDF bodies (MuJoCo C-library boundary, env RPC boundary, policy inference boundary) where the Daft executor has already materialised the batch. The AST checker auto-exempts to_pylist calls on named parameters of @daft.method.batch functions but cannot trace through this plain helper indirection; the call is semantically equivalent to an auto-sanctioned UDF-boundary access."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 38
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 38
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 95
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 140
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 190
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 234
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 239
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 239
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 270
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 274
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 274
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 303
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 309
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 309
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 316
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 316
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 349
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 363
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 367
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 367
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 396
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 410
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 414
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 414
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 418
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 418
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 450
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 458
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_cached_store_behavior.py"
line = 458
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_cached_store_behavior.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_processor.py"
line = 15
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_processor.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_processor.py"
line = 40
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_processor.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_processor.py"
line = 44
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_processor.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_processor.py"
line = 44
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_processor.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 79
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 79
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 85
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 85
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 91
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 92
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 93
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 104
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 126
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 138
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_query_updater.py"
line = 138
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_query_updater.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 96
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 96
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 148
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 156
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 157
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 182
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 188
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 188
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 192
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 192
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 198
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 198
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 219
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 227
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 227
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 262
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 270
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_store.py"
line = 271
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_store.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 85
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 85
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 118
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 118
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 129
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 129
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 200
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 200
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 212
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_system_negative_and_physics.py"
line = 212
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_system_negative_and_physics.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_edges.py"
line = 56
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_edges.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_edges.py"
line = 56
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_edges.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_edges.py"
line = 69
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_edges.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_edges.py"
line = 69
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_edges.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_edges.py"
line = 133
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_edges.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_edges.py"
line = 133
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_edges.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_edges.py"
line = 147
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_edges.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_execution.py"
line = 120
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_execution.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_execution.py"
line = 120
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_execution.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_execution.py"
line = 222
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_execution.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_execution.py"
line = 222
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_execution.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_execution.py"
line = 223
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_execution.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_execution.py"
line = 223
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_execution.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_lifecycle.py"
line = 83
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_lifecycle.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_lifecycle.py"
line = 83
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_lifecycle.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_lifecycle.py"
line = 87
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_lifecycle.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_lifecycle.py"
line = 88
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_lifecycle.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_lifecycle.py"
line = 89
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_lifecycle.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_lifecycle.py"
line = 104
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_lifecycle.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_lifecycle.py"
line = 104
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_lifecycle.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 90
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 90
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 97
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 97
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 121
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 121
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 127
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 127
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 147
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 147
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 152
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 152
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 194
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 194
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 204
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 204
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 219
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_mutations.py"
line = 219
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_mutations.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_querying.py"
line = 75
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_querying.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_querying.py"
line = 75
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_querying.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_querying.py"
line = 79
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_querying.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_querying.py"
line = 79
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_querying.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_querying.py"
line = 86
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_querying.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_querying.py"
line = 103
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_querying.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_querying.py"
line = 103
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_querying.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_querying.py"
line = 109
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_querying.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_async_world_querying.py"
line = 109
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_async_world_querying.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_storage_context_namespace_isolation.py"
line = 44
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_storage_context_namespace_isolation.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_storage_context_namespace_isolation.py"
line = 49
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_storage_context_namespace_isolation.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_storage_context_namespace_isolation.py"
line = 49
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_storage_context_namespace_isolation.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_storage_context_namespace_isolation.py"
line = 53
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_storage_context_namespace_isolation.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/aio/test_storage_context_namespace_isolation.py"
line = 53
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/aio"
rationale = "Assertion boundary in tests/aio/test_storage_context_namespace_isolation.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/api/test_query_filter.py"
line = 26
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/api"
rationale = "Assertion boundary in tests/api/test_query_filter.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/api/test_query_filter.py"
line = 26
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/api"
rationale = "Assertion boundary in tests/api/test_query_filter.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/api/test_query_filter.py"
line = 35
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/api"
rationale = "Assertion boundary in tests/api/test_query_filter.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/api/test_query_filter.py"
line = 35
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/api"
rationale = "Assertion boundary in tests/api/test_query_filter.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/api/test_query_filter.py"
line = 41
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/api"
rationale = "Assertion boundary in tests/api/test_query_filter.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/api/test_query_filter.py"
line = 41
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/api"
rationale = "Assertion boundary in tests/api/test_query_filter.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/physical_ai/test_instruction_sweep.py"
line = 170
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app/physical_ai"
rationale = "Assertion boundary in tests/app/physical_ai/test_instruction_sweep.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/physical_ai/test_instruction_sweep.py"
line = 170
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app/physical_ai"
rationale = "Assertion boundary in tests/app/physical_ai/test_instruction_sweep.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/physical_ai/test_instruction_sweep.py"
line = 342
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app/physical_ai"
rationale = "Assertion boundary in tests/app/physical_ai/test_instruction_sweep.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/physical_ai/test_instruction_sweep.py"
line = 342
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app/physical_ai"
rationale = "Assertion boundary in tests/app/physical_ai/test_instruction_sweep.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/physical_ai/test_task_evaluation.py"
line = 115
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app/physical_ai"
rationale = "Assertion boundary in tests/app/physical_ai/test_task_evaluation.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/physical_ai/test_task_evaluation.py"
line = 115
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app/physical_ai"
rationale = "Assertion boundary in tests/app/physical_ai/test_task_evaluation.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/physical_ai/test_task_evaluation.py"
line = 154
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app/physical_ai"
rationale = "Assertion boundary in tests/app/physical_ai/test_task_evaluation.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/physical_ai/test_task_evaluation.py"
line = 154
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app/physical_ai"
rationale = "Assertion boundary in tests/app/physical_ai/test_task_evaluation.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_application_contracts.py"
line = 61
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_application_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 100
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 123
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 191
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 383
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 391
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 400
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 409
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 422
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 431
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 447
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_ingestion.py"
line = 464
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_artifact_runtime_sync.py"
line = 25
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_artifact_runtime_sync.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_atomic_visibility.py"
line = 61
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_atomic_visibility.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_atomic_visibility.py"
line = 187
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_atomic_visibility.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_atomic_visibility.py"
line = 330
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_atomic_visibility.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_atomic_visibility.py"
line = 341
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_atomic_visibility.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 92
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 123
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 125
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 131
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 137
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 157
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 181
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 184
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 186
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_audit_contracts.py"
line = 189
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_audit_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 91
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 102
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 114
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 118
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 127
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 173
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 271
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 363
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 392
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 394
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 396
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 427
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 446
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 448
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 474
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 497
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 499
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_autoresearch_ledger.py"
line = 558
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_autoresearch_ledger.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_durable_commands.py"
line = 299
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_durable_commands.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_durable_commands.py"
line = 324
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_durable_commands.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_durable_commands.py"
line = 389
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_durable_commands.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_durable_commands.py"
line = 389
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_durable_commands.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_durable_discovery.py"
line = 337
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_durable_discovery.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_durable_discovery.py"
line = 417
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_durable_discovery.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_durable_discovery.py"
line = 609
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_durable_discovery.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_receipts.py"
line = 72
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_receipts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_receipts.py"
line = 116
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_receipts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_receipts.py"
line = 139
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_receipts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_receipts.py"
line = 175
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_receipts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_receipts.py"
line = 365
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_receipts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_receipts.py"
line = 400
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_receipts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_receipts.py"
line = 493
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_receipts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_receipts.py"
line = 567
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_receipts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_receipts.py"
line = 573
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_receipts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_service.py"
line = 33
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_service.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_eval_service.py"
line = 33
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_eval_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_ingestion_service.py"
line = 45
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_ingestion_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_ingestion_service.py"
line = 83
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_ingestion_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 75
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 75
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 122
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 122
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 229
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 229
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 241
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 241
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 308
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 308
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 350
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_mutation_service.py"
line = 350
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_mutation_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_remote_catalog_parity.py"
line = 511
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_remote_catalog_parity.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_remote_catalog_parity.py"
line = 514
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_remote_catalog_parity.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_autoresearch.py"
line = 55
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_autoresearch.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_autoresearch.py"
line = 68
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_autoresearch.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_autoresearch.py"
line = 87
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_autoresearch.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_autoresearch.py"
line = 168
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_autoresearch.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_autoresearch.py"
line = 181
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_autoresearch.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_contracts.py"
line = 157
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_contracts.py"
line = 182
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_contracts.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_contracts.py"
line = 182
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_contracts.py"
line = 198
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_contracts.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_contracts.py"
line = 198
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_fork_storage.py"
line = 65
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_fork_storage.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_fork_storage.py"
line = 86
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_fork_storage.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_fork_storage.py"
line = 114
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_fork_storage.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_fork_storage.py"
line = 122
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_fork_storage.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_fork_storage.py"
line = 167
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_fork_storage.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_runtime_fork_storage.py"
line = 211
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_runtime_fork_storage.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_spawn_audit_metadata.py"
line = 64
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_spawn_audit_metadata.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_storage_execution.py"
line = 38
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_storage_execution.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_storage_execution.py"
line = 61
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_storage_execution.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_storage_execution.py"
line = 118
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_storage_execution.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_storage_execution.py"
line = 121
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_storage_execution.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_storage_execution.py"
line = 151
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_storage_execution.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_storage_execution.py"
line = 182
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_storage_execution.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_storage_execution.py"
line = 261
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_storage_execution.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_trajectory_service.py"
line = 21
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_trajectory_service.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_trajectory_service.py"
line = 21
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_trajectory_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_transcript_ingestion.py"
line = 87
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_transcript_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_transcript_ingestion.py"
line = 90
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_transcript_ingestion.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_transcript_ingestion.py"
line = 164
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_transcript_ingestion.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_transcript_ingestion.py"
line = 165
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_transcript_ingestion.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_transcript_ingestion.py"
line = 179
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_transcript_ingestion.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_transcript_ingestion.py"
line = 180
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_transcript_ingestion.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_world_resume.py"
line = 90
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_world_resume.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_world_resume.py"
line = 150
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_world_resume.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_world_resume.py"
line = 199
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_world_resume.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_world_resume.py"
line = 405
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_world_resume.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_world_resume.py"
line = 418
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_world_resume.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_world_resume.py"
line = 471
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_world_resume.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/app/test_world_resume.py"
line = 514
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/app"
rationale = "Assertion boundary in tests/app/test_world_resume.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/artifacts/test_context_analysis.py"
line = 45
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/artifacts"
rationale = "Assertion boundary in tests/artifacts/test_context_analysis.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/artifacts/test_context_analysis.py"
line = 90
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/artifacts"
rationale = "Assertion boundary in tests/artifacts/test_context_analysis.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/artifacts/test_context_analysis.py"
line = 127
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/artifacts"
rationale = "Assertion boundary in tests/artifacts/test_context_analysis.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_duplicate_spawn_overwrite.py"
line = 42
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_duplicate_spawn_overwrite.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_error_propagation.py"
line = 90
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_error_propagation.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_error_propagation.py"
line = 96
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_error_propagation.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_error_propagation.py"
line = 126
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_error_propagation.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_matrix.py"
line = 111
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_matrix.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_matrix.py"
line = 116
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_matrix.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_matrix.py"
line = 121
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_matrix.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_matrix.py"
line = 126
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_matrix.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_matrix.py"
line = 131
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_matrix.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_matrix.py"
line = 136
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_matrix.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_matrix.py"
line = 137
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_matrix.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_union.py"
line = 39
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_union.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_union.py"
line = 46
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_union.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_async_world_get_components_union.py"
line = 47
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_async_world_get_components_union.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_batch_spawn_contract.py"
line = 88
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_batch_spawn_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_batch_spawn_contract.py"
line = 93
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_batch_spawn_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_batch_spawn_contract.py"
line = 152
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_batch_spawn_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_batch_spawn_contract.py"
line = 153
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_batch_spawn_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_batch_spawn_contract.py"
line = 279
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_batch_spawn_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_batch_spawn_contract.py"
line = 283
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_batch_spawn_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_batch_spawn_contract.py"
line = 375
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_batch_spawn_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_footgun_fixes.py"
line = 100
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_footgun_fixes.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_footgun_fixes.py"
line = 100
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_footgun_fixes.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_footgun_fixes.py"
line = 110
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_footgun_fixes.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_footgun_fixes.py"
line = 110
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_footgun_fixes.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_footgun_fixes.py"
line = 149
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_footgun_fixes.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_footgun_fixes.py"
line = 149
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_footgun_fixes.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_initial_conditions_contract.py"
line = 54
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_initial_conditions_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_initial_conditions_contract.py"
line = 87
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_initial_conditions_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_initial_conditions_contract.py"
line = 91
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_initial_conditions_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_resources_hooks_messaging.py"
line = 109
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_resources_hooks_messaging.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_resources_hooks_messaging.py"
line = 487
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_resources_hooks_messaging.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_resources_hooks_messaging.py"
line = 487
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_resources_hooks_messaging.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_resources_hooks_messaging.py"
line = 510
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_resources_hooks_messaging.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_resources_hooks_messaging.py"
line = 510
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_resources_hooks_messaging.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_same_tick_mutation_composition.py"
line = 52
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_same_tick_mutation_composition.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_same_tick_mutation_composition.py"
line = 61
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_same_tick_mutation_composition.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_same_tick_mutation_composition.py"
line = 84
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_same_tick_mutation_composition.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_same_tick_mutation_composition.py"
line = 103
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_same_tick_mutation_composition.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/core/test_same_tick_mutation_composition.py"
line = 127
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/core"
rationale = "Assertion boundary in tests/core/test_same_tick_mutation_composition.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 108
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 170
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 183
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 192
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 196
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 204
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 216
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 225
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 229
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 237
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 242
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 299
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 304
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 331
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/examples/test_biome_rts_contract.py"
line = 348
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/examples"
rationale = "Assertion boundary in tests/examples/test_biome_rts_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/experiments/test_tragedy_of_the_commons.py"
line = 48
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/experiments"
rationale = "Assertion boundary in tests/experiments/test_tragedy_of_the_commons.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/experiments/test_tragedy_of_the_commons.py"
line = 48
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/experiments"
rationale = "Assertion boundary in tests/experiments/test_tragedy_of_the_commons.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_cascade_contract.py"
line = 94
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_cascade_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_cascade_contract.py"
line = 122
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_cascade_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_depth_contract.py"
line = 49
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_depth_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_depth_contract.py"
line = 152
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_depth_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 63
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 97
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 98
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 117
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 119
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 141
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 145
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 168
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 235
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 240
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 257
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 275
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 367
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 381
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_edge_contract.py"
line = 388
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_edge_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_graph_view_contract.py"
line = 56
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_graph_view_contract.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_graph_view_contract.py"
line = 85
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_graph_view_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_graph_view_contract.py"
line = 125
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_graph_view_contract.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_graph_view_contract.py"
line = 128
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_graph_view_contract.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_graph_view_contract.py"
line = 162
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_graph_view_contract.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_graph_view_contract.py"
line = 186
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_graph_view_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_graph_view_contract.py"
line = 207
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_graph_view_contract.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 82
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 88
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 91
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 114
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 120
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 124
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 152
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 191
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 220
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 299
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 331
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 334
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 368
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 371
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 492
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 544
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_prefab_contract.py"
line = 546
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_prefab_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/graph/test_traverse_contract.py"
line = 51
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/graph"
rationale = "Assertion boundary in tests/graph/test_traverse_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_artifact_context.py"
line = 279
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_artifact_context.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_artifact_context.py"
line = 304
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_artifact_context.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_artifact_context.py"
line = 314
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_artifact_context.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_artifact_context.py"
line = 324
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_artifact_context.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_artifact_context.py"
line = 335
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_artifact_context.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_artifact_context.py"
line = 344
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_artifact_context.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_artifact_context.py"
line = 364
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_artifact_context.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_artifact_context.py"
line = 376
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_artifact_context.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_idempotency.py"
line = 98
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_idempotency.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_idempotency.py"
line = 106
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_idempotency.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/infrastructure/test_r2_idempotency.py"
line = 114
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/infrastructure"
rationale = "Assertion boundary in tests/infrastructure/test_r2_idempotency.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/ingestion/test_metadata_scans.py"
line = 57
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/ingestion"
rationale = "Assertion boundary in tests/ingestion/test_metadata_scans.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/ingestion/test_persistence_integrity.py"
line = 85
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/ingestion"
rationale = "Assertion boundary in tests/ingestion/test_persistence_integrity.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/ingestion/test_persistence_integrity.py"
line = 86
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/ingestion"
rationale = "Assertion boundary in tests/ingestion/test_persistence_integrity.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/ingestion/test_persistence_integrity.py"
line = 100
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/ingestion"
rationale = "Assertion boundary in tests/ingestion/test_persistence_integrity.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/ingestion/test_persistence_integrity.py"
line = 104
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/ingestion"
rationale = "Assertion boundary in tests/ingestion/test_persistence_integrity.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/ingestion/test_persistence_integrity.py"
line = 124
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/ingestion"
rationale = "Assertion boundary in tests/ingestion/test_persistence_integrity.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/ingestion/test_persistence_integrity.py"
line = 125
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/ingestion"
rationale = "Assertion boundary in tests/ingestion/test_persistence_integrity.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/ingestion/test_persistence_integrity.py"
line = 155
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/ingestion"
rationale = "Assertion boundary in tests/ingestion/test_persistence_integrity.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/ingestion/test_persistence_integrity.py"
line = 159
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/ingestion"
rationale = "Assertion boundary in tests/ingestion/test_persistence_integrity.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 234
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 240
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 252
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 253
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 255
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 259
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 260
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 261
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 263
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_agent_mission_service.py"
line = 267
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_agent_mission_service.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_command_flow.py"
line = 108
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_command_flow.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_command_flow.py"
line = 138
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_command_flow.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_command_flow.py"
line = 290
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_command_flow.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_command_flow.py"
line = 358
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_command_flow.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_episode_rollout.py"
line = 178
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_episode_rollout.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 98
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 99
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 168
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 181
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 205
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 213
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 284
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 299
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 325
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 333
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 380
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 389
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 433
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 442
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 483
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 526
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 557
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 587
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_fork_destroy_contracts.py"
line = 651
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_fork_destroy_contracts.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_pr_triage_example.py"
line = 42
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_pr_triage_example.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/integration/test_pr_triage_example.py"
line = 42
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/integration"
rationale = "Assertion boundary in tests/integration/test_pr_triage_example.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 97
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 98
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 107
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 108
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 126
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 127
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 133
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 134
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 150
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/missions/planning/test_htn_contract.py"
line = 151
method = "to_pydict"
boundary_kind = "test-assertion"
owner = "tests/missions/planning"
rationale = "Assertion boundary in tests/missions/planning/test_htn_contract.py: executes the Daft to_pydict terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_boundary_helper.py"
line = 119
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_boundary_helper.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_boundary_helper.py"
line = 119
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_boundary_helper.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_chunk_policy.py"
line = 161
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_chunk_policy.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_chunk_policy.py"
line = 327
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_chunk_policy.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_evaluation_integration.py"
line = 102
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_evaluation_integration.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_evaluation_integration.py"
line = 102
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_evaluation_integration.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_frame_ref_carriage.py"
line = 72
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_frame_ref_carriage.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_manipulation_harness.py"
line = 48
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_manipulation_harness.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_mujoco_rollout.py"
line = 79
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_mujoco_rollout.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_mujoco_rollout.py"
line = 110
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_mujoco_rollout.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/physical_ai/test_policy_loop.py"
line = 93
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/physical_ai"
rationale = "Assertion boundary in tests/physical_ai/test_policy_loop.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/projections/test_overview_contract.py"
line = 55
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/projections"
rationale = "Assertion boundary in tests/projections/test_overview_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/projections/test_overview_contract.py"
line = 74
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/projections"
rationale = "Assertion boundary in tests/projections/test_overview_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/projections/test_overview_contract.py"
line = 107
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/projections"
rationale = "Assertion boundary in tests/projections/test_overview_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/projections/test_overview_contract.py"
line = 132
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/projections"
rationale = "Assertion boundary in tests/projections/test_overview_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/projections/test_possession_contract.py"
line = 51
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/projections"
rationale = "Assertion boundary in tests/projections/test_possession_contract.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/storage/test_lancedb_store_core.py"
line = 40
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/storage"
rationale = "Assertion boundary in tests/storage/test_lancedb_store_core.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/storage/test_lancedb_store_core.py"
line = 88
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/storage"
rationale = "Assertion boundary in tests/storage/test_lancedb_store_core.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/storage/test_lancedb_store_core.py"
line = 110
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/storage"
rationale = "Assertion boundary in tests/storage/test_lancedb_store_core.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/storage/test_lancedb_store_open_and_optimize.py"
line = 37
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/storage"
rationale = "Assertion boundary in tests/storage/test_lancedb_store_open_and_optimize.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/storage/test_lancedb_store_open_and_optimize.py"
line = 104
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/storage"
rationale = "Assertion boundary in tests/storage/test_lancedb_store_open_and_optimize.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/storage/test_storage_roundtrip.py"
line = 113
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/storage"
rationale = "Assertion boundary in tests/storage/test_storage_roundtrip.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/storage/test_storage_roundtrip.py"
line = 113
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/storage"
rationale = "Assertion boundary in tests/storage/test_storage_roundtrip.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/storage/test_storage_roundtrip.py"
line = 155
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/storage"
rationale = "Assertion boundary in tests/storage/test_storage_roundtrip.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/storage/test_storage_roundtrip.py"
line = 155
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/storage"
rationale = "Assertion boundary in tests/storage/test_storage_roundtrip.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 74
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 74
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 232
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 232
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 332
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 332
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 381
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 382
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 412
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 412
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 428
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 428
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 465
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 465
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_stack_contracts.py"
line = 669
method = "count_rows"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_stack_contracts.py: executes the Daft count_rows terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_world.py"
line = 324
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_world.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_world.py"
line = 336
method = "collect"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_world.py: executes the Daft collect terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_world.py"
line = 336
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_world.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "tests/sync/test_sync_world.py"
line = 360
method = "to_pylist"
boundary_kind = "test-assertion"
owner = "tests/sync"
rationale = "Assertion boundary in tests/sync/test_sync_world.py: executes the Daft to_pylist terminal so the focused test can compare concrete results; this execution is owned by the repository test harness."

[[entries]]
path = "bench/core/ecs/add_remove.py"
line = 27
method = "collect"
boundary_kind = "benchmark-measurement"
owner = "bench/core/ecs"
rationale = "Measurement boundary in bench/core/ecs/add_remove.py: executes the Daft collect terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/core/ecs/add_remove.py"
line = 28
method = "to_arrow"
boundary_kind = "benchmark-measurement"
owner = "bench/core/ecs"
rationale = "Measurement boundary in bench/core/ecs/add_remove.py: executes the Daft to_arrow terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/core/ecs/add_remove.py"
line = 46
method = "collect"
boundary_kind = "benchmark-measurement"
owner = "bench/core/ecs"
rationale = "Measurement boundary in bench/core/ecs/add_remove.py: executes the Daft collect terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/core/ecs/add_remove.py"
line = 47
method = "to_arrow"
boundary_kind = "benchmark-measurement"
owner = "bench/core/ecs"
rationale = "Measurement boundary in bench/core/ecs/add_remove.py: executes the Daft to_arrow terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/core/ecs/entity_cycle.py"
line = 28
method = "collect"
boundary_kind = "benchmark-measurement"
owner = "bench/core/ecs"
rationale = "Measurement boundary in bench/core/ecs/entity_cycle.py: executes the Daft collect terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/core/ecs/entity_cycle.py"
line = 29
method = "count_rows"
boundary_kind = "benchmark-measurement"
owner = "bench/core/ecs"
rationale = "Measurement boundary in bench/core/ecs/entity_cycle.py: executes the Daft count_rows terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/core/ecs/entity_cycle.py"
line = 44
method = "collect"
boundary_kind = "benchmark-measurement"
owner = "bench/core/ecs"
rationale = "Measurement boundary in bench/core/ecs/entity_cycle.py: executes the Daft collect terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/core/ecs/entity_cycle.py"
line = 45
method = "to_arrow"
boundary_kind = "benchmark-measurement"
owner = "bench/core/ecs"
rationale = "Measurement boundary in bench/core/ecs/entity_cycle.py: executes the Daft to_arrow terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/core/query_latency.py"
line = 182
method = "collect"
boundary_kind = "benchmark-measurement"
owner = "bench/core"
rationale = "Measurement boundary in bench/core/query_latency.py: executes the Daft collect terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/core/query_latency.py"
line = 182
method = "count_rows"
boundary_kind = "benchmark-measurement"
owner = "bench/core"
rationale = "Measurement boundary in bench/core/query_latency.py: executes the Daft count_rows terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/observability/daft_attribution.py"
line = 365
method = "collect"
boundary_kind = "benchmark-measurement"
owner = "bench/observability"
rationale = "Measurement boundary in bench/observability/daft_attribution.py: executes the Daft collect terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "bench/observability/daft_attribution.py"
line = 369
method = "to_pylist"
boundary_kind = "benchmark-measurement"
owner = "bench/observability"
rationale = "Measurement boundary in bench/observability/daft_attribution.py: executes the Daft to_pylist terminal as the operation or validated result measured by this repository benchmark."

[[entries]]
path = "evals/infra/idempotency_worker.py"
line = 118
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/infra"
rationale = "Evaluation boundary in evals/infra/idempotency_worker.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/infra/idempotency_worker.py"
line = 157
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/infra"
rationale = "Evaluation boundary in evals/infra/idempotency_worker.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/infra/idempotency_worker.py"
line = 183
method = "count_rows"
boundary_kind = "evaluation-oracle"
owner = "evals/infra"
rationale = "Evaluation boundary in evals/infra/idempotency_worker.py: executes the Daft count_rows terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 194
method = "collect"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft collect terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 194
method = "to_pydict"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pydict terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 259
method = "collect"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft collect terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 259
method = "to_pydict"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pydict terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 380
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 498
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 509
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 517
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 645
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 657
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 664
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 679
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/capability/tasks.py"
line = 680
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/capability"
rationale = "Evaluation boundary in evals/suites/capability/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/durable.py"
line = 64
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/durable.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/durable.py"
line = 298
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/durable.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/durable.py"
line = 340
method = "count_rows"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/durable.py: executes the Daft count_rows terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/durable.py"
line = 370
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/durable.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 544
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 609
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 675
method = "count_rows"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft count_rows terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 676
method = "count_rows"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft count_rows terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 709
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 757
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 790
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 927
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 935
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 978
method = "collect"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft collect terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 979
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 989
method = "collect"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft collect terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 990
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 1002
method = "collect"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft collect terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 1003
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 1053
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 1054
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 1055
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/idempotency/tasks.py"
line = 1056
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/idempotency"
rationale = "Evaluation boundary in evals/suites/idempotency/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/regression/tasks.py"
line = 410
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/regression"
rationale = "Evaluation boundary in evals/suites/regression/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/regression/tasks.py"
line = 421
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/regression"
rationale = "Evaluation boundary in evals/suites/regression/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/regression/tasks.py"
line = 432
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/regression"
rationale = "Evaluation boundary in evals/suites/regression/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "evals/suites/regression/tasks.py"
line = 745
method = "to_pylist"
boundary_kind = "evaluation-oracle"
owner = "evals/suites/regression"
rationale = "Evaluation boundary in evals/suites/regression/tasks.py: executes the Daft to_pylist terminal so the repository scenario can grade a concrete externally observable result."

[[entries]]
path = "examples/00_quickstart.py"
line = 33
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/00_quickstart.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/00_quickstart.py"
line = 33
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/00_quickstart.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/01_world_mutations.py"
line = 90
method = "show"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/01_world_mutations.py: executes the Daft show terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/01_world_mutations.py"
line = 99
method = "show"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/01_world_mutations.py: executes the Daft show terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/01_world_mutations.py"
line = 111
method = "show"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/01_world_mutations.py: executes the Daft show terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/01_world_mutations.py"
line = 131
method = "count_rows"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/01_world_mutations.py: executes the Daft count_rows terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/02_fork_counterfactual.py"
line = 70
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/02_fork_counterfactual.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/02_fork_counterfactual.py"
line = 112
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/02_fork_counterfactual.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/02_fork_counterfactual.py"
line = 122
method = "count_rows"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/02_fork_counterfactual.py: executes the Daft count_rows terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/03_time_travel.py"
line = 47
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/03_time_travel.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/04_messaging.py"
line = 102
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/04_messaging.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/04_messaging.py"
line = 102
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/04_messaging.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/04_messaging.py"
line = 287
method = "show"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/04_messaging.py: executes the Daft show terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/04_messaging.py"
line = 295
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/04_messaging.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/04_messaging.py"
line = 296
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/04_messaging.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/05_llm_agents.py"
line = 125
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/05_llm_agents.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/05_llm_agents.py"
line = 125
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/05_llm_agents.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/06_trajectory_analysis.py"
line = 119
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/06_trajectory_analysis.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/06_trajectory_analysis.py"
line = 119
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/06_trajectory_analysis.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/06_trajectory_analysis.py"
line = 123
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/06_trajectory_analysis.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/06_trajectory_analysis.py"
line = 123
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/06_trajectory_analysis.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/07_hooks.py"
line = 115
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/07_hooks.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/07_hooks.py"
line = 115
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/07_hooks.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/07_hooks.py"
line = 176
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/07_hooks.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/07_hooks.py"
line = 177
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/07_hooks.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/08_htn_resolution.py"
line = 140
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/08_htn_resolution.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/08_htn_resolution.py"
line = 140
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/08_htn_resolution.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/08_htn_resolution.py"
line = 202
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/08_htn_resolution.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/08_htn_resolution.py"
line = 202
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/08_htn_resolution.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/08_htn_resolution.py"
line = 251
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/08_htn_resolution.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/08_htn_resolution.py"
line = 252
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/08_htn_resolution.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/09_cloud_storage.py"
line = 241
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/09_cloud_storage.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/09_cloud_storage.py"
line = 241
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/09_cloud_storage.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/10_autoresearch.py"
line = 55
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/10_autoresearch.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/10_autoresearch.py"
line = 69
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/10_autoresearch.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/10_autoresearch.py"
line = 108
method = "show"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/10_autoresearch.py: executes the Daft show terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/11_graph_relationships.py"
line = 57
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/11_graph_relationships.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/11_graph_relationships.py"
line = 62
method = "count_rows"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/11_graph_relationships.py: executes the Daft count_rows terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/12_prefabs.py"
line = 63
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/12_prefabs.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/12_prefabs.py"
line = 73
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/12_prefabs.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/13_biome_rts.py"
line = 82
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/13_biome_rts.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/13_biome_rts.py"
line = 154
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/13_biome_rts.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/13_biome_rts.py"
line = 157
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/13_biome_rts.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/13_biome_rts.py"
line = 162
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/13_biome_rts.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/13_biome_rts.py"
line = 165
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/13_biome_rts.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/13_biome_rts.py"
line = 182
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/13_biome_rts.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/13_biome_rts.py"
line = 196
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/13_biome_rts.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/13_biome_rts.py"
line = 201
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/13_biome_rts.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/pr_triage.py"
line = 241
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/pr_triage.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/pr_triage.py"
line = 241
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/pr_triage.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/simulation_script.py"
line = 100
method = "collect"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/simulation_script.py: executes the Daft collect terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/simulation_script.py"
line = 101
method = "to_pylist"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/simulation_script.py: executes the Daft to_pylist terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "examples/simulation_script.py"
line = 118
method = "show"
boundary_kind = "example-output"
owner = "examples"
rationale = "User-facing example boundary in examples/simulation_script.py: executes the Daft show terminal to render or inspect the concrete result taught by the runnable example."

[[entries]]
path = "experiments/cooperative_emergence.py"
line = 153
method = "to_pydict"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/cooperative_emergence.py: executes the Daft to_pydict terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 119
method = "to_pylist"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft to_pylist terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 128
method = "to_pylist"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft to_pylist terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 135
method = "to_pylist"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft to_pylist terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 143
method = "to_pylist"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft to_pylist terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 151
method = "to_pylist"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft to_pylist terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 153
method = "count_rows"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft count_rows terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 154
method = "count_rows"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft count_rows terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 161
method = "to_pylist"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft to_pylist terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 163
method = "count_rows"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft count_rows terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 164
method = "count_rows"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft count_rows terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/session_reflection.py"
line = 169
method = "to_pylist"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/session_reflection.py: executes the Daft to_pylist terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/tragedy_of_the_commons.py"
line = 85
method = "collect"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/tragedy_of_the_commons.py: executes the Daft collect terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/tragedy_of_the_commons.py"
line = 165
method = "collect"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/tragedy_of_the_commons.py: executes the Daft collect terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/tragedy_of_the_commons.py"
line = 166
method = "to_arrow"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/tragedy_of_the_commons.py: executes the Daft to_arrow terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/tragedy_of_the_commons.py"
line = 293
method = "collect"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/tragedy_of_the_commons.py: executes the Daft collect terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "experiments/tragedy_of_the_commons.py"
line = 293
method = "to_pylist"
boundary_kind = "experiment-observation"
owner = "experiments"
rationale = "Experimental observation boundary in experiments/tragedy_of_the_commons.py: executes the Daft to_pylist terminal to inspect the concrete outcome of this non-product experiment."

[[entries]]
path = "scripts/validate_r2_substrate.py"
line = 118
method = "to_pylist"
boundary_kind = "repository-validation"
owner = "scripts"
rationale = "Repository tooling boundary in scripts/validate_r2_substrate.py: executes the Daft to_pylist terminal to validate or report a concrete checked-in harness result."

[[entries]]
path = "scripts/validate_r2_substrate.py"
line = 145
method = "count_rows"
boundary_kind = "repository-validation"
owner = "scripts"
rationale = "Repository tooling boundary in scripts/validate_r2_substrate.py: executes the Daft count_rows terminal to validate or report a concrete checked-in harness result."
