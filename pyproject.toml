[project]
name = "archetype-ecs"
version = "0.1.1"
description = "Dataframe-first, append-only ECS runtime for simulations and AI agents. Built on Daft with LanceDB time-travel storage."
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
    { name = "Vangelis Technologies Inc.", email = "everett@vangelis.tech" },
]
keywords = ["simulation", "ecs", "reinforcement-learning", "llm", "daft", "lancedb"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "daft[openai,lance,iceberg]>=0.7.15",
    "lancedb>=0.22.0",
    "pyiceberg[daft,sql-sqlite]",
    "uuid-utils>=0.11.0",
    "psutil>=5.9",
    "pydantic>=2.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "typer>=0.9",
    "httpx>=0.27",
    "logfire>=4.32.0",
    "opentelemetry-instrumentation-fastapi>=0.60b1",
    "opentelemetry-instrumentation-httpx>=0.60b1",
    "opentelemetry-instrumentation-jinja2>=0.60b1",
    "opentelemetry-instrumentation-requests>=0.60b1",
    "opentelemetry-instrumentation-sqlalchemy>=0.60b1",
    "opentelemetry-instrumentation-sqlite3>=0.60b1",
    "opentelemetry-instrumentation-urllib>=0.60b1",
]

[project.scripts]
archetype = "archetype.cli.main:app"

[project.optional-dependencies]
benchmark  = ["esper>=2.45"]
sim        = ["mujoco>=3.2"]
otel       = ["opentelemetry-api>=1.20", "opentelemetry-sdk>=1.20", "opentelemetry-exporter-otlp-proto-grpc>=1.20"]
dev        = [
    "pytest>=8.3", "pytest-asyncio>=0.26", "pytest-cov>=5.0",
    "ruff>=0.15,<0.16",  # pinned: 0.15 introduced I001/B007 rule changes; keep local == CI
    "httpx>=0.27",
    "matplotlib>=3.9",
    "viztracer>=1.0.4",
    "ipykernel",
    "mutmut>=3.5",
]
docs       = [
    "mkdocs-shadcn",
    "mkdocs-mermaid2-plugin",
    "mkdocstrings[python]>=0.27",
    "pymdown-extensions",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]


[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
pythonpath = [
    "src",
    ".",
]
asyncio_default_fixture_loop_scope = "function"

[tool.coverage.run]
branch = true
source = ["archetype"]
omit = [
    "tests/*",
    # Re-export stubs
    "src/archetype/core/storage/storage.py",
]
disable_warnings = [
    "module-not-measured",
]

[tool.coverage.report]
show_missing = true
skip_covered = true
precision = 1
fail_under = 70

[tool.coverage.xml]
output = "coverage.xml"

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-asyncio>=0.26",
    "pytest-cov>=5.0",
    "ruff>=0.15,<0.16",  # pinned: 0.15 introduced I001/B007 rule changes; keep local == CI
    "httpx>=0.27",
    "pre-commit>=4.0",
    "mutmut>=3.5",
    "mujoco>=3.2",  # physics parity tests (tests/experiments/test_mujoco_rollout.py)
    "pytest-xdist>=3.8.0",
]

# Mutation testing. Scoped narrowly: mutmut runs each mutation as a full
# test process, so a whole-codebase sweep can take hours. The pilot scope
# below is intentionally tight — expand it as the workflow proves out.
# See docs/guide/mutation-testing.md for usage and the rationale.
[tool.mutmut]
paths_to_mutate = [
    "src/archetype/core/component.py",
    "src/archetype/core/archetype.py",
]
tests_dir = [
    "tests/core/test_component_core.py",
    "tests/core/test_archetype_core_signatures.py",
]
# pytest's [tool.pytest.ini_options].pythonpath puts mutants/src first on
# sys.path, so the rest of the archetype package has to be staged into
# mutants/ as well — otherwise tests/conftest.py fails to import
# archetype.app. mutmut writes the mutated component.py *after*
# also_copy runs, so the unmutated copy here is harmlessly overwritten.
also_copy = [
    "src/archetype",
]
# Run only the pilot's tests, quietly. The default test selection is
# the whole tests/ tree, which would re-run the full suite per mutant.
pytest_add_cli_args = ["-q", "--no-header", "-x"]
do_not_mutate = [
    "*/__init__.py",
]

[tool.uv]
# Supply-chain guard: refuse packages released after this date (7-day quarantine).
# Bump this manually as needed; uv 0.8.x doesn't support relative durations.
exclude-newer = "2026-06-12T00:00:00Z"

[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = [
    ".venv",
    "archetype_data",
    ".archetype_meta",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "E501",   # Line too long (handled by formatter)
]

[tool.ruff.lint.per-file-ignores]
"src/archetype/api/routes/*.py" = ["B008"]  # Depends() in defaults is standard FastAPI

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ty.environment]
python-version = "3.12"

[tool.ty.src]
# ty follows imports from a project root rather than honoring pre-commit's
# `files:` filter, so scope the checked source set here. This keeps ty off
# bench/evals/experiments/examples/scripts and the vendored .venv/stdlib stubs.
include = ["src"]
respect-ignore-files = true

[tool.ty.rules]
# Optional deps imported lazily inside functions (e.g. matplotlib in periphery).
unresolved-import = "ignore"

# Every other rule runs at its default ("error") level — the codebase is clean.
# Known false positives carry a targeted inline `# ty: ignore[rule]` with a
# comment naming the cause (mostly Daft stubs typing Expression.__eq__/__gt__
# as bool, plus a few deliberate hasattr/duck-typing probes). Suppress new
# findings inline the same way — do not down-level rules here.
